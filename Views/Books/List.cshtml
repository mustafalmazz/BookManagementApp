@model IEnumerable<Book>
@{
    ViewData["Title"] = "Book List";
}

<h1 class="text-center">@ViewData["Title"]</h1>
<hr />
<a href="/books/create" class="btn btn-success mb-2">Add New</a>

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Image</th>
            <th>Name</th>
            <th>Author</th>
            <th>Price</th>
            <th>Description</th>
            <th>Category</th>
            
            <th style="width:150px;">Operations</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td><img src="@item.Image" style="width:100px"/></td>
                <td>@item.Name</td>
                <td>@item.Author</td>
                <td>₺@item.Price</td>
                <td>@item.Description</td>
                <td>@item.Category?.CategoryName</td>
                <td>
                    <a href="/Books/Delete/@item.Id" class="btn btn-danger btn-sm">Delete</a>
                    <a href="/Books/Edit/@item.Id" class="btn btn-primary btn-sm">Edit</a>
                </td>
            </tr>
        }
    </tbody>
</table>


